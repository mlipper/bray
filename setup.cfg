#
# This file is modeled after the format suggested by the Python Packaging
# Authority (pypa) in a sample project found here:
#
#   https://github.com/pypa/sampleproject.git
#
# branch master
# commit 52966defd6a61e97295b0bb82cd3474ac3e11c7a
#

[metadata]
# This includes the license file(s) in the wheel.
# https://wheel.readthedocs.io/en/stable/user_guide.html#including-license-files-in-the-generated-wheel-file
license_files = LICENSE.txt

[bdist_wheel]
universal = 1

[flake8]
max-line-length = 140
exclude = .tox,.eggs,ci/templates,build,dist

#
# The configuration for pytest has been moved to pytest.ini.
#
# This section was generated by the cookiecutter-pylibrary.
# Since I'm not sure whether moving it was silly, this
# commented configuration is kept until I understand more.
#

# [tool:pytest]
# # If a pytest section is found in one of the possible config files
# # (pytest.ini, tox.ini or setup.cfg), then pytest will not look for any others,
# # so if you add a pytest config section elsewhere,
# # you will need to delete this section from setup.cfg.
# norecursedirs =
#     migrations
#
# python_files =
#     test_*.py
#     *_test.py
#     tests.py
# addopts =
#     -ra
#     --strict
#     --doctest-modules
#     --doctest-glob=\*.rst
#     --tb=short
# testpaths =
#     tests

# [tool:isort]
# force_single_line = True
# line_length = 120
# known_first_party = bray
# default_section = THIRDPARTY
# forced_separate = test_bray
# skip = .tox,.eggs,ci/templates,build,dist
